/** Vagrant.ai, the company behind v-sigma. */
export const VAGRANT_URL = "https://vagrant.ai";

/**
 * Vagrant.ai on GitHub — the org, not a repository. What the header's mark points
 * at: the site's own source isn't the interesting destination, the org's work is.
 */
export const VAGRANT_GITHUB = {
  name: "Vagrant.ai",
  url: "https://github.com/vagrant-ai",
} as const;

export const CONTACT_EMAIL = "contact@vagrant.ai";

/** Nebula, the open-source project v-sigma is built on. */
export const NEBULA = {
  name: "Nebula",
  url: "https://github.com/InftyAI/Nebula",
} as const;
